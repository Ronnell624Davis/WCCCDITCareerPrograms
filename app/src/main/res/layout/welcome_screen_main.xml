<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/woodbackground"
    android:weightSum="1"
    tools:context="com.wcccd.wcccditcareerprograms.WelcomeScreenMainActivity">

    <Button
        android:id="@+id/tapToViewButton"
        android:layout_width="160dp"
        android:layout_height="90dp"
        android:layout_marginTop="61dp"
        android:onClick="tapToViewPrograms"
        android:text="@string/welcome_button"
        android:textSize="18sp"
        android:layout_below="@+id/welcome_text_view"
        android:layout_alignStart="@+id/wcccdImageView" />

    <ImageView
        android:id="@+id/wcccdImageView"
        android:layout_width="400dp"
        android:layout_height="400dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="35dp"
        app:srcCompat="@drawable/iti_web_logo" />

    <TextView
        android:id="@+id/welcome_text_view"
        android:layout_width="473dp"
        android:layout_height="163dp"
        android:layout_marginTop="35dp"
        android:text="@string/welcome_screen"
        android:textAlignment="center"
        android:textAppearance="@style/TextAppearance.AppCompat.Title"
        android:textColor="@android:color/white"
        android:textSize="30sp"
        android:textStyle="normal|bold"
        android:layout_below="@+id/wcccdImageView"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/contactButton"
        android:layout_width="160dp"
        android:layout_height="90dp"
        android:onClick="contactButton"
        android:text="@string/ContactButton"
        android:textSize="18sp"
        android:layout_alignTop="@+id/tapToViewButton"
        android:layout_alignEnd="@+id/wcccdImageView" />
</RelativeLayout>

